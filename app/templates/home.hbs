<!-- Fixed navbar -->
    <div class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">FIFA World Cup</a>
        </div>
        <div class="navbar-collapse collapse">
          <ul class="nav navbar-nav">
            {{#link-to "groups" tagName="li" }}<a>Groups</a>{{/link-to}}
            {{#link-to "fixtures" tagName="li" }}<a>Fixtures</a>{{/link-to}}
            <li><a href="#contact">Simulate</a></li>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">My Favourites <b class="caret"></b></a>
              <ul class="dropdown-menu">
              {{#if controllers.favourites.teams}}
                <li class="dropdown-header">Teams</li>
                {{#each team in controllers.favourites.teams}}
                  <li><a href="#">{{team.title}}</a></li>
                {{/each}}
              {{/if}}
              {{#if controllers.favourites.matches}}
                <li class="divider"></li>
                <li class="dropdown-header">Matches</li>
                {{#each match in controllers.favourites.matches}}
                  <li><a href="#">{{match.title}}</a></li>
                {{/each}}
              {{/if}}
              </ul>
            </li>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
    <div class="banner">
      <img src="assets/banner.png">  
    </div>
    
    <div class="container theme-showcase" role="main">
      
{{outlet}}
</div>